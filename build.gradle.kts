// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Versions are managed via pluginManagement in settings.gradle.kts
}

allprojects {
    // Common configuration can go here
}
